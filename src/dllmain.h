#include <pch.h>

/**
 * @brief Paths to the game exe and this dll.
 * @note May be an empty path if unable to pull this data.
 */
extern std::filesystem::path exe_path;
extern std::filesystem::path dll_path;
